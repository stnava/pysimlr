Paper content regressed during stability refactoring. Real-world application section must be expanded to include all architectural variants (SiMLR, LEND, NED, NEDPP). Need to restore depth to meet 5-10 page per section targets.  The weight in the NSA flow algorithm (nsa_w or just w) varies only between 0 and 1.  0.1 (recommended default), 0.2, 0.5 and 0.9 are relevant values.
